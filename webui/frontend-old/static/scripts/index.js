// Nav Bar


// Boxes
let activeTasks = new Vue({
    el: '#active-tasks-number',
    data: {
        number: '-'
    }
})

let onexecutionTasks = new Vue({
    el: '#onexecution-tasks-number',
    data: {
        number: '-'
    }
})

let inactiveTasks = new Vue({
    el: '#inactive-tasks-number',
    data: {
        number: '-'
    }
})